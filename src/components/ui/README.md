# ui

앱 전역에서 재사용하는 기본 UI 컴포넌트를 관리합니다.

- 버튼, 입력창, 모달, 카드처럼 도메인에 종속되지 않은 컴포넌트를 둡니다.
- 여러 기본 UI를 조합한 기능별 컴포넌트는 해당 `features` 폴더에 둡니다.
- 컴포넌트의 플랫폼별 구현이 필요하면 `.ios.tsx`, `.android.tsx`, `.web.tsx` 확장자를 사용합니다.
